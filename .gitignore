node_modules/
dist/
.env
*.db
*.sqlite
.DS_Store
coverage/
*.db-shm
*.db-wal

# Runtime config — rewritten by the app at runtime whenever bots/strategies change.
# Each has a committed .example counterpart; copy it on first run if the real file is absent.
config/bots.json
config/custom-strategies.json
config/lanes.json
config/server.json
config/snapshot.json
config/cex-wallets.json

# Runtime logs
logs/

# Private working docs — never committed (public repo)
BACKLOG.md
IDEAS.md
